<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container">
		<a class="navbar-brand" href="{{ path('app_home', {_locale: app.request.locale}) }}">TodoList</a>
		<div class="d-flex gap-2">
			{% if is_granted('IS_AUTHENTICATED_FULLY') %}
				<a href="{{ path('app_logout', {_locale: app.request.locale}) }}" class="btn btn-danger" role="button">{% trans from 'messages' %}button.signout{% endtrans %}</a>
				<img class="img-fluid ms-4" style="max-height : 50px;max-width : 50px;" src="{{ asset( app.user.avatar) }}" alt="Avatar">
			{% else %}
				<a href="{{ path('app_login', {_locale: app.request.locale}) }}" class="btn btn-success" role="button">{% trans from 'messages' %}button.signin{% endtrans %}</a>
				<a href="{{ path('app_register', {_locale: app.request.locale}) }}" class="btn btn-info" role="button">{% trans from 'messages' %}button.signup{% endtrans %}</a>
			{% endif %}


			<div class="dropdown">
				<a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
					{{ app.request.locale|locale_name(app.request.locale)|u.title }}
				</a>
				<div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
					<a class="dropdown-item" href="{{ path('app_home', {_locale: 'en'}) }}">English</a>
					<a class="dropdown-item" href="{{ path('app_home', {_locale: 'fr'}) }}">Français</a>
				</div>
			</div>

		</div>
	</div>
</nav>
